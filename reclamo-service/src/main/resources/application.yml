spring:
    application:
        name: reclamo-service
    config:
        import:
        - optional:configserver:http://localhost:8888/
    cloud:
        config:
            username: root
            password: piero