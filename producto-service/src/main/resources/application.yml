server:
    port: 9002
spring:
    jpa: 
        hibernate:
            ddl-auto: update
        show-sql: true
    datasource:
        url: jdbc:mysql://localhost:3306/asistencia
        username: root
        password: piero

    application:
        name: producto-service
    config:
        import:
        - optional:configserver:http://localhost:8888/
    cloud:
        config:
            username: root
            password: piero