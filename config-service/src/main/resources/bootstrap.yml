server: 
    port: 8001
spring:
    cloud:
        config:
            server:
                git:
                    uri: https://gitlab.com/pierotoscano1496/java-spring-caso-practico.git
                    search-paths:
                    - config-data
                    username: ${GITLAB_USER}
                    password: ${GITLAB_PASSWORD}
    security:
        user:
            name: root
            password: piero