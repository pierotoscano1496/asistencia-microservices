server:
    port: 8888
spring:
    cloud:
        config:
            server:
                git:
                    uri: file:///${user.home}/Desktop/Proyectos/asistencia-rest
                    search-paths:
                    - config-data
    security:
        user:
            name: root
            password: piero