server:
    port: 8888
spring:
    cloud:
        config:
            server:
                git:
                    # uri: https://github.com/pierotoscano1496/asistencia-microservices.git
                    # search-paths:
                    # - config-data
                    # username: ${GIT_USER}
                    # password: ${GIT_PASSWORD}
                    uri: file:///${user.home}/Desktop/spring-cloud-server-repositories/asistencia
    security:
        user:
            name: root
            password: piero